clc
format short
x=[0 1 2 4 5 6];
y=[1 14 5 5 6 19];
X=
n=size(x);
for i=2:n
    for j=i:n
        DD(j,i)=(DD(j,i-1)-DD(j-1,-1))/(x(j)-x(j-i+1));
    end
end
for i=1:n
    prod(i)=1;
    for j=1:i-1
        prod(i)=prod(i)*()
